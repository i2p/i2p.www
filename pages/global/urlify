{% macro urlify(url, title, suffix) %}
   {% autoescape false %}
    {% if static %}
    <a href="{{url}}.{{suffix}}">{{title}}</a>
    {% else %}
    <a href="{{url}}">{{title}}</a>
    {% endif %}
   {% endautoescape %}
{% endmacro %}